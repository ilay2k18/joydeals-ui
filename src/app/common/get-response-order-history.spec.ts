import { GetResponseOrderHistory } from './get-response-order-history';

describe('GetResponseOrderHistory', () => {
  it('should create an instance', () => {
    expect(new GetResponseOrderHistory()).toBeTruthy();
  });
});
