import { JoyDealsValidators } from './joy-deals-validators';

describe('JoyDealsValidators', () => {
  it('should create an instance', () => {
    expect(new JoyDealsValidators()).toBeTruthy();
  });
});
